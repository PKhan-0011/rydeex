import React from "react";

interface propsTypes {
  open: boolean;
  onClose: () => void;
}

// props aya hai ek tarikhe ka okkh!..

const AuthModal = ({ open, onClose }: propsTypes) => {
  return <div></div>;
};

export default AuthModal;
